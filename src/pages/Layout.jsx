import NavBar from "../components/landing/NavBar";

export default function Layout({ children }) {
    return (
        <div>
            {/* <NavBar /> */}
            {children}
        </div>
    )
}
